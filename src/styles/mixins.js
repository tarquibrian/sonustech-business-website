import { css } from "styled-components"

const mixins = {
  boxSection: css`
    padding: 2rem;
    background-color: #f233;
    /* min-height: 100%; */
    margin: 0 auto;
    max-width: 1400px;
  `,
}

export default mixins
